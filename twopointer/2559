import sys
input = sys.stdin.readline 

n, k = map(int, input().split())
nums = list(map(int, input().split()))

each = sum(nums[0:k])
maxv = each

for i in range(k, n):
    each += nums[i]
    each -= nums[i-k]
    maxv = max(maxv, each)
    
print(maxv)